# LLM + embeddings (optional for Phase 1 vector build — Chroma default works offline)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Tracing (Phase 4+)
LANGCHAIN_TRACING_V2=false
LANGCHAIN_API_KEY=
LANGCHAIN_PROJECT=claims-multiagent-rag-poc

# Paths (defaults usually fine)
CLAIMS_POLICY_CONFIG=config/claims_policy.yml
CHROMA_PERSIST_DIR=.cache/chroma
AUDIT_DB_PATH=.cache/audit_trail.sqlite
