# Copy to .env and adjust if running services on non-default ports.
# Never commit a real .env — this file is tracked only as a template.

# --- Authorization Server ---
AUTH_ISSUER=http://localhost:8000
MCP_AUDIENCE=http://localhost:8001/mcp
ACCESS_TOKEN_TTL=300

# --- Claims MCP Server ---
EXPECTED_ISSUER=http://localhost:8000
EXPECTED_AUDIENCE=http://localhost:8001/mcp
JWKS_URL=http://localhost:8000/jwks.json

# --- Client ---
MCP_URL=http://localhost:8001/mcp
MCP_TOKEN_FILE=.claims_mcp_tokens.json
